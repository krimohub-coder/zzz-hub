-- DELETE OLD
for _,v in pairs(game.CoreGui:GetChildren()) do
	if v.Name == "zzz_hub" then
		v:Destroy()
	end
end

-- GUI
local gui = Instance.new("ScreenGui", game.CoreGui)
gui.Name = "zzz_hub"

-- MAIN
local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 230, 0, 260)
main.Position = UDim2.new(0.5,-115,0.5,-130)
main.BackgroundColor3 = Color3.fromRGB(15,15,20)
main.Active = true
main.Draggable = true -- ✅ DRAG FIX

Instance.new("UICorner", main)

-- TITLE
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1,0,0,30)
title.BackgroundTransparency = 1
title.Text = "zzz hub"
title.TextColor3 = Color3.fromRGB(0,170,255)
title.Font = Enum.Font.GothamBold

-- LAYOUT
local layout = Instance.new("UIListLayout", main)
layout.Padding = UDim.new(0,5)

-- TOGGLE FUNCTION
local function createToggle(name)
	local btn = Instance.new("TextButton", main)
	btn.Size = UDim2.new(1,-10,0,30)
	btn.BackgroundColor3 = Color3.fromRGB(25,25,30)
	btn.TextColor3 = Color3.new(1,1,1)
	btn.Font = Enum.Font.Gotham
	btn.Text = name.." : OFF"

	local state = false

	btn.MouseButton1Click:Connect(function()
		state = not state
		btn.Text = name.." : "..(state and "ON" or "OFF")
		btn.BackgroundColor3 = state and Color3.fromRGB(0,170,255) or Color3.fromRGB(25,25,30)
	end)
end

-- SLIDER FUNCTION (WORKING)
local UIS = game:GetService("UserInputService")

local function createSlider(name, min, max, start)
	local frame = Instance.new("Frame", main)
	frame.Size = UDim2.new(1,-10,0,40)
	frame.BackgroundColor3 = Color3.fromRGB(25,25,30)

	local label = Instance.new("TextLabel", frame)
	label.Size = UDim2.new(1,0,0,20)
	label.BackgroundTransparency = 1
	label.Text = name..": "..start
	label.TextColor3 = Color3.new(1,1,1)

	local bar = Instance.new("Frame", frame)
	bar.Size = UDim2.new(1,-10,0,10)
	bar.Position = UDim2.new(0,5,0,25)
	bar.BackgroundColor3 = Color3.fromRGB(60,60,60)

	local fill = Instance.new("Frame", bar)
	fill.Size = UDim2.new((start-min)/(max-min),0,1,0)
	fill.BackgroundColor3 = Color3.fromRGB(0,170,255)

	local dragging = false

	bar.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
		end
	end)

	UIS.InputEnded:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end
	end)

	UIS.InputChanged:Connect(function(i)
		if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then
			local pos = (i.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X
			pos = math.clamp(pos,0,1)

			fill.Size = UDim2.new(pos,0,1,0)

			local val = math.floor(min + (max-min)*pos)
			label.Text = name..": "..val
		end
	end)
end

-- BUILD
createSlider("Speed", 10, 100, 20)
createToggle("Auto Farm")
createToggle("Infinite Jump")
createToggle("FPS Boost")
